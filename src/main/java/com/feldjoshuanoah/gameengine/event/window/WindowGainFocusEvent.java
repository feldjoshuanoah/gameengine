package com.feldjoshuanoah.gameengine.event.window;

import com.feldjoshuanoah.gameengine.event.Event;

/**
 * Represents a window gain focus event that is fired when the window gains focus.
 */
public final class WindowGainFocusEvent implements Event {

    /**
     * Create a new window gain focus event.
     */
    public WindowGainFocusEvent() {
    }
}
